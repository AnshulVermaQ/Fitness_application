spring:
  application:
    name: config-server
  profiles:
    active: production
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

server:
  port: 8889